name: Hello

on:
  push:
    branches: [main]

jobs:

  greet:
    runs-on: ubuntu-latest

    steps:
      -name: say hello
       run: echo "hello dosto"

      -name: say hello to username
       run: echo "hey user"
